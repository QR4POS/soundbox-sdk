## 1.0.0

- Initial release
- `SoundboxClient.createVoice()` — get signed audio URL
- `SoundboxPlayer.play()` — one-call audio playback (Flutter)
- Supports Sinhala, English, Tamil
- Typed exceptions for auth, rate limit, network errors
